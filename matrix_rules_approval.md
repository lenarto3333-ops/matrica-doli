# Матриця долі — специфікація розрахунку та візуалізації

> Статус документа: проєкт для погодження  
> Версія: 1.0  
> Предметна модель: «Матриця долі», система 22 арканів  
> Джерело правил: метод Наталії Ладіні, редакція через «силу роду»; контрольна візуалізація — Avatarium

## 1. Мета й область застосування

Цей документ визначає:

- формат вхідних даних;
- правило редукції чисел до діапазону 1–22;
- порядок розрахунку всіх точок Матриці долі;
- значення точок і зон;
- геометричну структуру схеми;
- кольори та правила візуального оформлення;
- вимоги до перевірки правильності реалізації.

Документ призначений для погодження предметної логіки та подальшої реалізації калькулятора й графічної схеми.

Документ не визначає тексти інтерпретацій окремих арканів. Вони мають бути описані окремо.

## 2. Терміни та позначення

| Термін | Визначення |
| :--- | :--- |
| Аркан | Ціле число від 1 до 22, отримане безпосередньо з дати або шляхом редукції. |
| Редукція | Повторне складання цифр числа, доки результат не стане меншим або дорівнюватиме 22. |
| Точка | Розраховане значення, що має унікальне позначення та позицію на схемі. |
| Особистісний квадрат | Основний квадрат із вершинами `a`, `b`, `c`, `d`. |
| Родовий квадрат | Квадрат із вершинами `f`, `g`, `y`, `k`, повернений на 45° відносно особистісного. |
| Лінія Неба | Вертикальна вісь від `b` до `d`. |
| Лінія Землі | Горизонтальна вісь від `a` до `c`. |
| Кармічний хвіст | Нижня послідовність точок `d₂ → d₁ → d`. |
| Канал грошей | Послідовність точок у правій нижній частині матриці, пов’язана з `c₂`, `x₂`, `x`. |
| Канал стосунків | Послідовність точок у правій нижній частині матриці, пов’язана з `d₁`, `x₁`, `x`. |

У формулах використовується функція `R(X)`, визначена в розділі 4.

## 3. Вхідні дані та валідація

### 3.1. Формат

Вхідними даними для числового розрахунку є дата народження. Стать може передаватися окремо для текстових інтерпретацій, але не змінює числа матриці.

```text
DD.MM.YYYY
```

де:

- `DD` — день народження;
- `MM` — місяць народження;
- `YYYY` — чотиризначний рік народження.

Початкові нулі в дні та місяці не впливають на розрахунок: `03` дорівнює `3`, `08` дорівнює `8`.

Стать передається одним із двох нормативних значень:

- `man` — чоловік;
- `woman` — жінка.

Стать не бере участі у формулах матриці, зокрема у формулах родових діагоналей.

### 3.2. Перевірка дати

Перед розрахунком система повинна перевірити, що:

- усі три компоненти є цілими числами;
- дата існує в календарі;
- рік записаний чотирма цифрами;
- значення не містять додаткових символів.
- обрано одне з допустимих значень статі.

Якщо дата некоректна, розрахунок не виконується, а користувач отримує повідомлення про помилку.

## 4. Правило редукції

### 4.1. Нормативне визначення

Функція `R(X)` приводить додатне ціле число до діапазону від 1 до 22:

1. Якщо `X ≤ 22`, результат дорівнює `X`.
2. Якщо `X > 22`, цифри числа додаються.
3. Якщо отримана сума знову більша за 22, операція повторюється.

Формально:

```text
R(X) = X,                         якщо 1 ≤ X ≤ 22
R(X) = R(сума десяткових цифр X), якщо X > 22
```

Це правило не є математичною операцією `X mod 22` і не повинно реалізовуватися через остачу від ділення.

### 4.2. Приклади

| Вхідне число | Обчислення | Результат |
| ---: | :--- | ---: |
| 7 | `7` | 7 |
| 22 | `22` | 22 |
| 23 | `2 + 3` | 5 |
| 39 | `3 + 9` | 12 |
| 1999 | `1 + 9 + 9 + 9 = 28`, `2 + 8 = 10` | 10 |

## 5. Реєстр точок

### 5.1. Основні точки

| Точка | Назва або роль | Залежить від |
| :---: | :--- | :--- |
| `a` | День народження; ліва вершина; зона ресурсу; 0 років | `DD` |
| `b` | Місяць народження; верхня вершина; головний талант; 20 років | `MM` |
| `c` | Рік народження; права вершина; завдання душі; 40 років | `YYYY` |
| `d` | Нижня вершина; головне опрацювання; 60 років | `a`, `b`, `c` |
| `e` | Центр; зона комфорту душі | `a`, `b`, `c`, `d` |

### 5.2. Родові точки

| Точка | Позиція та роль | Залежить від |
| :---: | :--- | :--- |
| `f` | Верхня ліва вершина; чоловіча родова лінія | `a`, `b` |
| `g` | Верхня права вершина; жіноча родова лінія | `b`, `c` |
| `y` | Нижня права вершина; чоловіча родова лінія | `c`, `d` |
| `k` | Нижня ліва вершина; жіноча родова лінія | `a`, `d` |

### 5.3. Проміжні та підсумкові точки

| Група | Точки | Призначення |
| :--- | :--- | :--- |
| Вертикальна вісь | `b₁`, `b₂`, `b₃`, `d₁`, `d₂` | Внутрішні точки Лінії Неба |
| Горизонтальна вісь | `a₁`, `a₂`, `a₃`, `c₁`, `c₂` | Внутрішні точки Лінії Землі |
| Канали | `x`, `x₁`, `x₂` | Баланс грошей і стосунків, канал стосунків, канал грошей |
| Особисте призначення | `m`, `h`, `j` | Небо, Земля та підсумок |
| Соціальне призначення | `t`, `n`, `z` | Чоловічий рід, жіночий рід та підсумок |
| Духовне призначення | `s` | Загальний підсумок призначень |

## 6. Формули розрахунку

Формули наведені в обов’язковому порядку залежностей.

### 6.1. Базові точки

Сумою цифр року позначено додавання всіх чотирьох цифр `YYYY`.

```text
a = R(DD)
b = R(MM)
c = R(сума цифр YYYY)
d = R(a + b + c)
e = R(a + b + c + d)
```

### 6.2. Родовий квадрат

```text
f = R(a + b)
g = R(b + c)
y = R(c + d)
k = R(a + d)
```

### 6.2.1. Внутрішні точки родових діагоналей

Застосовується редакція методу Ладіні через **силу роду**, як у контрольному калькуляторі Avatarium. Спочатку сила роду обчислюється як редукована сума чотирьох кутів родового квадрата:

```text
q = R(f + g + y + k)
```

Для кожного кута внутрішня точка дорівнює сумі кута із силою роду, а зовнішня — сумі кута з отриманою внутрішньою точкою:

```text
nwInner = R(f + q);  nwOuter = R(f + nwInner)
neInner = R(g + q);  neOuter = R(g + neInner)
swInner = R(k + q);  swOuter = R(k + swInner)
seInner = R(y + q);  seOuter = R(y + seInner)
```

Порядок кожної пари у програмному інтерфейсі: `[внутрішня, зовнішня]`, тобто від центра до кутової точки.

Редакція через зону комфорту (`R(e + кут)`) у цьому проєкті не використовується. Вона є іншою версією методики й для частини дат дає інші значення.

Контрольні значення:

| Дата | Сила роду | NW | NE | SW | SE |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `24.09.1973` | `5` | `20, 8` | `16, 9` | `19, 6` | `15, 7` |
| `29.06.2008` | `9` | `8, 7` | `7, 5` | `11, 4` | `10, 11` |

### 6.3. Лінія Неба

Розрахунок виконується від зовнішніх точок до центру:

```text
b₂ = R(b + e)
b₁ = R(b + b₂)
b₃ = R(b₂ + e)

d₁ = R(d + e)
d₂ = R(d + d₁)
```

### 6.4. Лінія Землі

```text
a₂ = R(a + e)
a₁ = R(a + a₂)
a₃ = R(a₂ + e)

c₂ = R(c + e)
c₁ = R(c + c₂)
```

### 6.5. Канали грошей і стосунків

```text
x  = R(d₁ + c₂)
x₂ = R(x + c₂)
x₁ = R(x + d₁)
```

Семантичні ролі:

- `x` — точка балансу грошей і стосунків;
- `x₂` — енергія грошового каналу;
- `x₁` — енергія каналу стосунків;
- `c₂` — вхід у грошовий канал;
- `c₁` — зовнішня проміжна точка на правій горизонтальній осі.

### 6.6. Кармічний хвіст

Кармічний хвіст утворюють три точки:

```text
d₂ → d₁ → d
```

де `d` є головною точкою, а `d₁` і `d₂` — супровідними.

### 6.7. Особисте призначення

Період: до 40 років.

```text
m = R(b + d)  — Небо
h = R(a + c)  — Земля
j = R(m + h)  — особисте призначення
```

### 6.8. Соціальне призначення

Період: від 40 до 60 років.

```text
t = R(f + y)  — чоловічий рід
n = R(g + k)  — жіночий рід
z = R(t + n)  — соціальне призначення
```

### 6.9. Духовне призначення

Період: після 60 років.

```text
s = R(j + z)
```

## 7. Семантичне значення зон

| Зона | Основні точки | Значення |
| :--- | :--- | :--- |
| Особистість і ресурс | `a` | Базовий ресурс, прояв особистості, початкова життєва точка |
| Головний талант | `b` | Природний талант і верхня точка розвитку |
| Завдання душі | `c` | Основне завдання та точка переходу близько 40 років |
| Головне опрацювання | `d` | Кармічна програма та точка переходу близько 60 років |
| Комфорт душі | `e` | Центральна енергія та звичний внутрішній стан |
| Чоловіча родова лінія | `f → e → y` | Програми чоловічої частини роду |
| Жіноча родова лінія | `g → e → k` | Програми жіночої частини роду |
| Грошовий канал | `c₂`, `x₂`, `x` | Фінансова реалізація та баланс матеріальної енергії |
| Канал стосунків | `d₁`, `x₁`, `x` | Партнерство та баланс у стосунках |
| Кармічний хвіст | `d₂`, `d₁`, `d` | Послідовність основних кармічних завдань |

Тексти трактувань значень арканів для кожної зони зберігаються окремо від формул.

## 8. Геометрія та розташування

### 8.1. Базова система координат

Для незалежності від розміру екрана використовується нормалізована координатна площина:

- центр схеми `e` має координати `(0, 0)`;
- повна ширина та висота основної матриці дорівнюють `2`;
- крайні координати лежать у діапазоні від `−1` до `1`.

### 8.2. Особистісний квадрат

| Точка | Координати | Позиція |
| :---: | :---: | :--- |
| `a` | `(−1, 0)` | ліворуч |
| `b` | `(0, −1)` | угорі |
| `c` | `(1, 0)` | праворуч |
| `d` | `(0, 1)` | унизу |
| `e` | `(0, 0)` | у центрі |

Точки `a`, `b`, `c`, `d` з’єднуються послідовно та утворюють квадрат, повернений відносно осей координат.

### 8.3. Родовий квадрат

| Точка | Координати | Позиція |
| :---: | :---: | :--- |
| `f` | `(−q, −q)` | угорі ліворуч |
| `g` | `(q, −q)` | угорі праворуч |
| `y` | `(q, q)` | унизу праворуч |
| `k` | `(−q, q)` | унизу ліворуч |

де `q = √2 / 2 ≈ 0,7071`.

Точки `f`, `g`, `y`, `k` утворюють квадрат, сторони якого паралельні осям координат. Разом обидва квадрати формують восьмикутну зірку.

### 8.4. Внутрішні точки

Усі проміжні точки розміщуються на відповідних відрізках у такій послідовності:

- Лінія Неба: `b → b₁ → b₂ → b₃ → e → d₂ → d₁ → d`;
- Лінія Землі: `a → a₁ → a₂ → a₃ → e → c₂ → c₁ → c`;
- чоловіча родова лінія: `f → e → y`;
- жіноча родова лінія: `g → e → k`.

Точні відстані між проміжними точками мають бути зафіксовані в окремому макеті інтерфейсу.

### 8.5. Блок призначень

Точки `m`, `h`, `j`, `t`, `n`, `z`, `s` розміщуються над основною матрицею у вигляді дуги:

- `m`, `h`, `j` — ліва частина, особисте призначення;
- `t`, `n`, `z` — права частина, соціальне призначення;
- `s` — найвища центральна точка, духовне призначення.

## 9. Кольори та стилі

Колір є властивістю типу точки, а не частиною формули.

| Точки або зона | Колір | Рекомендована роль |
| :--- | :--- | :--- |
| `e` | жовтий | центральна точка |
| `a`, `b` | фіолетовий або бузковий | верхня та ліва базові зони |
| `c`, `d` | червоний | праве та нижнє ключові завдання |
| `c₁`, `x₂`, `x`, `d₁`, `d₂` | помаранчевий | канали й проміжні кармічні точки |
| `f`, `g`, `y`, `k` | білий або світло-сірий із контуром | родові вершини |
| `j`, `z`, `s` | помаранчевий або сірий | підсумки призначень |

Для остаточної реалізації потрібно окремо затвердити точні значення кольорів у форматі HEX та їх контрастність на світлому й темному фоні.

## 10. Контрольний приклад

Для затвердження алгоритму необхідно додати щонайменше один повний приклад, який містить:

1. дату народження;
2. обчислення `a`, `b`, `c`, `d`, `e`;
3. обчислення родових точок;
4. обчислення всіх проміжних точок;
5. обчислення трьох призначень;
6. еталонне зображення готової матриці.

Контрольну дату та очікувані результати має підтвердити предметний експерт. До підтвердження цей розділ не є нормативним.

## 11. Граничні випадки та перевірки

### 11.1. Обов’язкові автоматичні перевірки

- кожна розрахована точка є цілим числом від 1 до 22;
- жодна формула не використовує ще не обчислену точку;
- для однакової дати система завжди повертає однакові числові значення незалежно від статі;
- початкові нулі не змінюють результат;
- високосна дата `29.02` приймається лише для високосного року;
- некоректні календарні дати відхиляються до початку розрахунку.

### 11.2. Мінімальний набір тестових дат

- дата з однозначними днем і місяцем;
- дата з днем `22`;
- дата з днем понад `22`;
- дата `29.02` високосного року;
- дата з роком, сума цифр якого дорівнює `22`;
- дата з роком, сума цифр якого перевищує `22`;
- некоректна дата.

## 12. Питання, що потребують затвердження

Перед наданням документу статусу «затверджено» необхідно погодити:

1. Чи правильно визначено редукцію як повторне складання цифр, а не операцію за модулем 22?
2. Чи є `x₁` саме точкою каналу стосунків, `x₂` — грошового каналу, а `c₂` — входом у грошовий канал?
3. Чи правильна послідовність проміжних точок на вертикальній і горизонтальній осях?
4. Чи відповідають назви та вікові періоди методології школи?
5. Чи потрібна редукція для дня народження, якщо `DD > 22`?
6. Чи затверджені назви всіх зон українською мовою?
7. Які точні HEX-коди кольорів потрібно використовувати?
8. Яка контрольна дата буде еталонною для перевірки всіх формул?
9. Який макет або зображення є еталоном геометричного розташування проміжних точок?

## 13. Критерії затвердження

Документ можна вважати затвердженим, коли:

- предметний експерт підтвердив усі формули;
- закрито питання з розділу 12;
- додано повний контрольний приклад;
- погоджено еталонну геометрію;
- погоджено палітру кольорів;
- документ має номер фінальної версії, дату затвердження та відповідальну особу.

### Лист погодження

| Роль | Ім’я | Рішення | Дата | Коментар |
| :--- | :--- | :--- | :--- | :--- |
| Предметний експерт |  |  |  |  |
| Власник продукту |  |  |  |  |
| Дизайнер |  |  |  |  |
| Технічний відповідальний |  |  |  |  |
